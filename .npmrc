# Strict by default: a package must declare what it imports.
auto-install-peers=true
strict-peer-dependencies=false
# Keep the store layout predictable in Docker builds.
node-linker=isolated
# pnpm 10 requires this for `pnpm deploy` to work from a workspace.
inject-workspace-packages=true
